FROM alpine:3.14.2

RUN apk --update --no-cache add \
                                ca-certificates \
                                git \
                                openssh \
                                openssl \
                                python3 \
                                ansible \
                                vim

ADD ./tmp/id_rsa_docker_test /root/.ssh/id_rsa

# ADD AUTOMATICALLY KEY
# RUN eval "$(ssh-agent -s)" && ssh-add /root/.ssh/id_rsa_docker_test